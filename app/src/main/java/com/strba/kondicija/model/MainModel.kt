package com.strba.kondicija.model

import com.strba.kondicija.Contract

class MainModel : Contract.Model {
    fun saveLogEntry(log: String) {
        // Save the log entry to the database or calendar
    }
}